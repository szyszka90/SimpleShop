{% extends "base.html.twig" %}

{% block body %}
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.description) }}
        {% for localePrice in form.prices %}
            {{ form_row(localePrice) }}
        {% endfor%}
    {{ form_end(form) }}
{% endblock %}